Put your app icon in this folder with this exact filename:

- `app-icon.png` is used for the browser tab icon, web app manifest, and iPhone home-screen icon.

Recommended image sizes:

- `app-icon.png`: square PNG, 1024x1024 preferred.

After replacing this file, run the frontend build again and upload the new `client/dist` folder to Netlify.
